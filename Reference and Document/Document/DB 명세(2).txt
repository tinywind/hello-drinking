1. 회원정보 테이블(User)
 (ID, Name, Password, photo, age, sex, phone_number, job, mini_home)

Create table User(
ID VARCHAR(32) NOT NULL PRIMARY KEY,
NAME VARCHAR(16) NOT NULL,
PASSWORD VARCHAR(32) NOT NULL
PHOTO blob,
AGE NUMBER,
PHONE_NUMBER NUMBER,
JOB VARCHAR(16),
MINI_HOMEPAGE VARCHAR(32),
LOG NUMBER
)

2. 게시물 테이블(Posts)
 (ID, name, photo, 인원, comment, date,matching)

Create table Posts(
ID VARCHAR(32) NOT NULL PRIMARY KEY,
Name VARCHAR(32) NOT NULL,
PHOTO blob
인원
comment VARCHAR(50),
date date,
matching 

)
3. 관심자 테이블(Follow)
(follower_id, follwing_id) 
Create Table Follw(
FOLLOWER_ID , 
FOLLOWING_ID
)